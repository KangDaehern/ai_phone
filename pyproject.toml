[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-phone"
version = "0.1.0"
description = "Small-office AI phone routing simulator"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
ai-phone = "ai_phone.cli:main"

[tool.setuptools]
packages = ["ai_phone"]
